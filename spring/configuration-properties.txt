@ConfigurationProperties is a powerful feature that allows you to map external configuration properties 
(such as properties in application.properties or application.yml) to a Java class in a type-safe way. 
This approach is useful for managing and accessing complex configuration data, especially when you want to group related properties together.

import org.springframework.boot.context.properties.ConfigurationProperties;
import org.springframework.stereotype.Component;

@Component
@ConfigurationProperties(prefix = "app")
public class AppConfig {
    private String name;
    private String version;
    private Features features;

    // Getters and setters
    public static class Features {
        private boolean enableFeatureX;

        // Getter and setter
        public boolean isEnableFeatureX() {
            return enableFeatureX;
        }

        public void setEnableFeatureX(boolean enableFeatureX) {
            this.enableFeatureX = enableFeatureX;
        }
    }

    // Getters and setters for name and version
    public String getName() {
        return name;
    }

    public void setName(String name) {
        this.name = name;
    }

    public String getVersion() {
        return version;
    }

    public void setVersion(String version) {
        this.version = version;
    }

    public Features getFeatures() {
        return features;
    }

    public void setFeatures(Features features) {
        this.features = features;
    }
}


Immutability and why only static nested classes for nested objects
